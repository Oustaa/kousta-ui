import { FC, useState } from "react";

type MenuProps = {
  hover?: boolean;
};

const Menu: FC<MenuProps> = ({ hover }) => {
  const [opend, closed] = useState(false);
  return <></>;
};

export default Menu;
